import requests

class GitHubJob:
    def __init__(self, url):
        self.url = 'https://jobs.github.com/api/positions.json'

    def procuraJSON():

        
    def chamar(self, termo, pagina):
        resposta =
        return requests.get(f'{self.url}?page={pagina}&search ={termo}')
